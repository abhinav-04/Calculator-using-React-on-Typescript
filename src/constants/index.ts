export const DIGITS = [7, 8, 9, 4, 5, 6, 1, 2, 3, 0];

export enum BINARY_OPS {
  ADD = "+",
  SUBTRACT = "-",
  DIVIDE = "/",
  MULTIPLY = "×",
  EQUAL = "=",
}

export enum UNARY_OPS {
  RESET = "AC",
  PERCENT = "%",
  NEGATE = "+/-",
}
